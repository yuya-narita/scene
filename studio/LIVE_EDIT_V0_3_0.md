# Live Edit v0.3.0

- Cursor/split explanation appears only once per browser (localStorage).
- Compact cursor dock reduced and moved to the lower-right above the iOS keyboard accessory.
- Empty Scenes receive a Live Edit-only invisible text hit target.
- Tapping an empty Scene opens inline editing and the iOS keyboard instead of advancing.
- Confirming with no text keeps the Scene empty.
