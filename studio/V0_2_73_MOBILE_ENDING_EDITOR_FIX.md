# v0.2.73
- Fixed `読了ページ` heading wrapping vertically on iPhone.
- Ending preview is now shown before the long legacy form.
- On iPhone, the long form is collapsed under `フォームで細かく編集`.
- Left/right sample boxes are always visible in the preview even when not configured.
- Tapping center / left / right opens a custom fixed overlay editor (no `<dialog>` dependency).
- Recent ending text/button presets remain stored in localStorage.
