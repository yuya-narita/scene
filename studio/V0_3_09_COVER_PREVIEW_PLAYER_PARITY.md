# v0.3.09 — Cover preview / Player parity

- Easy Studio's inline cover preview now uses the same centered vertical flow as the real Player cover.
- Removed the inline preview's absolute-positioned copy/start composition.
- Work title/logo, subtitle, author, episode, episode title, and start button now keep the same relative spacing as the Player.
- Logo and type sizes are scaled from the preview card width, so the small editor card remains a faithful miniature rather than a separately designed cover.
- Player behavior and published output are unchanged.
