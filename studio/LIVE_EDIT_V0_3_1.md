# Live Edit v0.3.1

- Cursor helpers are tucked into a tiny `↵|` trigger during inline writing.
- Tap the trigger to reveal `‹ / › / ‹。 / 。› / ↵|`; it auto-collapses after about 4.2 seconds.
- Removed repeated Scene-by-Scene cursor-help intro.
- `+` now inserts an empty Scene and synchronously enters inline edit so iPhone can open the keyboard in the same user gesture.
- Confirming without typing intentionally preserves an empty Scene.
