# v0.2.70

- Studio Player center `COVER / 表紙に戻る` now uses the same two-line structure, font family, size, letter spacing, color, and vertical alignment as the optional left/right boxes.
- Easy Studio ending miniature preview uses the same matching typography across all three boxes.
- No Public Player or Worker change is required.
