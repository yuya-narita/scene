# Live Edit v0.2.6

- Inline text editing now follows the caret above the iPhone keyboard using visualViewport.
- The existing ScenePlayerCore Auto Fit tiers are recalculated on every input, so editing and replay use the same text size immediately.
- No new text sizing rules were introduced.
