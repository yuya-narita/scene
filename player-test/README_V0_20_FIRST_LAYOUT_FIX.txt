player-test v0.20

Fixes first-visit foreground Scene image placement:
- waits for the image to decode/load
- re-measures the active Scene stack
- matches the already-correct position seen after returning from Past

Cloudflare ?src= loading is unchanged.
