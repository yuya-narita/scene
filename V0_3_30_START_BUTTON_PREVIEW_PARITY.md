# v0.3.30 — Start button preview parity

- Public Player cover start button now matches the Studio / work preview.
- Fixes the LIGHT + cover-image case where the button text became nearly invisible because `--public-text` and `--public-bg` both resolved to light colors.
- Uses the same compact rounded-rectangle silhouette instead of the older pill shape.
- Keeps the Mincho start label and the existing cover composition untouched.
