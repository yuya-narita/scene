# AHAKO V2.22 縦書き実装候補

V2.22を基準に、Scene本文へ書字方向を追加しました。

- 横書き（既存Sceneの既定値）
- 縦書き（右から左へ列送り）
- Sceneの積層方向は従来どおり上方向
- 横書きと縦書きのSceneを混在可能
- 短い縦書きは内容に応じた高さ、長文は画面高の約52%で列送り
- 縦書き時の左・中央・右は、文字の上下揃えではなくブロック全体の横位置
- 縦書き本文内部は上揃え
- PCとiPhoneで同じ相対位置になる物理左右マージン方式
- Scene送りは折り返し箱ではなく、実際に描画された文字領域を計測
- Sceneごとに「縦方向（上へ送る）／横方向（ページ送り）」を選択可能
- 新しく表示されるSceneの設定が、直前Sceneの移動方向を決定
- 縦書きを含む縦方向フローでは画面高に比例した追加余白を確保
- 横方向では、横書きなら前Sceneを左へ、縦書きなら前Sceneを右へ自動送出
- 横方向では直前Sceneだけを薄く残し、それ以前のSceneは再表示しない
- 書字方向とSceneの流れは独立して組み合わせ可能
- Sceneごとに「なし／手書き吹き出し」を選択可能
- 手書き吹き出しは横書き・縦書きの双方に対応
- 文字量と書字方向から余白込みの外形を自動計測
- 本文と輪郭の間に、iPhoneでも潰れない広めの上下左右余白を確保
- Scene IDと本文を種にして、Sceneごとに異なる非対称の輪郭を生成
- 同一Sceneでは再表示しても同じ輪郭を維持
- 主線・薄い滲み線・ずれた細線・部分的な掠れを重ねたSVG描画
- Scene配置は文字だけでなく吹き出し全体の外形を使って計算
- チャット表示には適用せず、既存チャット吹き出しを維持
- 縦書き吹き出しは全列の実描画幅を計測し、右端1列だけで枠幅が決まらないよう補正
- 縦書き吹き出しは文字の実描画上端・下端を測り、上下余白を同量に再構成
- 文字量・列数が増えるほど、楕円から手描き角丸四角へ連続的に変形
- 長文ほど上下左右の余白を段階的に拡張
- 現在と直前の吹き出しが実寸で画面内に収まる場合は2つを残す
- 2つが収まらない長文Sceneでは直前の吹き出しを隠し、現在Sceneを優先
- 縦書きの横方向フローは、直前を右・現在を左に配置して読書順を維持
- 吹き出しを「左上／上／右上／左／中央／右／左下／下／右下」へScene単位で配置可能
- プレビューをタップまたはドラッグして自由配置可能
- 自由配置はピクセル値ではなく画面内の比率で保存し、PCとiPhoneの座標差を抑制
- 配置時は文字と枠を別々に動かさず、計測済みの吹き出し全体を一体で移動
- 吹き出しは安全余白を含めて画面内に制限し、直前Sceneと重なる場合は現在Sceneを優先
- iPhoneで縦書きの列幅確定後に文字が再配置されても、最終字形を再計測して枠とのずれを防止
- 吹き出しのない通常テキストも、9方向プリセットと自由ドラッグ配置に対応
- 共通の配置値は本文設定へ保存し、既存の吹き出し専用配置値も後方互換で読み込み
- 「前の文章を残す」では長文でも直前の吹き出しを消さず、現在Sceneを前面にして重ねて表示
- 直前Sceneを残したくない場合はSceneの表示を「この文章だけ」に設定
- Desktop StudioのLive Previewに「iPhone／タブレット／PC」表示切替を追加
- iPhone表示は390:844、PC表示は16:9で実寸レイアウトを再計算し、公開Playerの位置確認に利用可能
- 中央線・三分割線・安全余白を重ねる安全ガイドを追加（端末切替・ガイド状態はこの端末だけに保存）
- 読了ページQuick EditのSEチェックボックスを標準サイズへ固定
- Desktop Studioの文字カードは内容増加時に高さを自動拡張し、パネル末尾までスクロール可能
- 端末プレビューは既存の固定Playerサイズより優先する実寸指定へ変更し、390:844／3:4／16:9を明確に切替
- 文字カードは「文字の詳細設定」を先頭へ出し、クイック設定を折りたたみ式にしてカード内への埋没を防止
- 端末切替はPlayer外側ではなく、実際の`.sp-core`を専用viewportへ格納して`min-height:100dvh`も上書き
- Studioスクリプトへ更新識別子を付け、HTMLだけ新しくJavaScriptが旧キャッシュのまま残る状態を防止
- 端末枠の中心はブラウザ全体ではなく、右編集パネルの左端から計測したプレビュー領域の中央へ固定
- Desktopの文字（Aa）カードはインライン展開を廃止し、独立した詳細設定画面を開く単一導線へ整理
- 2×2設定カードの行高を内容基準へ変更し、上段カードが下段の背景・音カードへ重ならないよう修正
- PC安全ガイドの内側線を縮小で消えるグラデーションから実線要素へ変更
- Scene背景の表示位置調整画面を、選択中のiPhone／タブレット／PCプレビュー比率へ同期
- 縦長背景をPCでcover表示した際も、実際の上下トリミングを見ながら縦横位置を調整可能
- 公開Playerの再生開始前背景にもScene 1の保存位置を先に適用し、中央から指定位置へ一瞬跳ねる表示を解消
- 導入中に進んだ背景モーションを画面へ出す前に初期化し、開始直後の一瞬の拡大・着地を解消
- 導入背景とScene本体背景の描画領域を同じ外周3%拡張へ統一し、レイヤー交代時の画角差を解消
- テキスト・吹き出し配置へ任意のPC専用座標を追加（iPhone／タブレットは従来どおり共通）
- PCプレビューのドラッグ調整だけがPC専用位置を保存し、未設定Sceneは共通位置へ自動フォールバック
- PC専用位置は「共通位置」で解除でき、共通配置を変更してもPC専用値を保持
- 公開CoreのPC幅判定を自身のPlayer幅から直接取得し、開始時の未定義メソッド例外を修正
- 文字の枠に、既存と同じ手描き線の四角い「手書きナレーション枠」を追加
- 共通の文字計測を使う試作枠として、角丸・二重線・点線・黒ベタ・半透明パネルを追加
- 表紙画像の表示位置をiPhone・タブレット・PC別に調整・保存できるよう拡張
- 背景切替時間を最大10秒へ拡張し、指定時間が終わるまで旧背景を保持する自然なクロスフェードへ修正
- 現在Sceneの背景画像を変えず、直前に表示されていた背景の表示位置と背景サイズだけをコピーする機能を追加
- 間に「前Sceneから継続」のSceneがあっても実際の背景まで遡り、同構図画像のクロスフェード位置を一致
- 背景カードの表示位置調整と前Scene位置コピーを小型の横並びボタンへ整理し、隣の音カードとの高さ差を縮小
- 背景カード全体を左サムネ・右4段操作へ再設計し、背景状態・位置・明暗・詳細設定を一つのコンパクトなブロックへ整理
- AUTO RECをScene間の相対タイマーだけでなく、Scene 1／音声開始を0秒とする絶対キュー（cueAt）も保存する方式へ変更
- AUTO再生は絶対キューとの差分を毎回再計算し、描画・レイアウト処理時間がSceneごとに累積するドリフトを解消
- タイプライター表示後も残り絶対時間だけを待ち、文字表示時間と記録pauseの二重加算を解消
- 従来のpauseは共鳴率・時間編集・旧Player互換のため維持し、旧作品は相対タイマー再生へ自動フォールバック
- 共鳴率の読者時間をScene描画完了後ではなく、タップ／キー／スワイプで進めた瞬間に記録する方式へ変更
- 共鳴率を選択式の難易度にはせず、共通の「標準判定（standard-v3）」へ統一
- 標準判定はSceneごとの間隔一致を75%、作品開始からの累積位置一致を25%として計算
- 全Scene平均70%＋得点下位20%の平均30%で集計し、長編で大きなズレが薄まる問題を抑制
- 許容幅を間隔 `max(0.8秒, 作者時間×35%)`、累積位置 `max(1.2秒, 作者時間×20%)` に調整
- 結果画面へ共鳴深度「余白／呼吸／共鳴／同期」を自動表示（62%／78%／90%を境界）
- Public Playerの操作不能なオープニング約690msをScene 1の読者時間から除外
- Scene積層の測定範囲を本文／文字枠だけでなく、サブテキスト・Scene画像・チャット行・音マークを含むScene全体へ拡張
- Scene画像の初回ロード後に画像寸法を含めて再配置し、次Sceneが画像領域へ重なる問題を修正
- 画像ロード後の再配置でもstack／overlayそれぞれの本来の配置経路を維持
- AUTO REC完了集計で最終Sceneの時間が二重加算される不具合を修正

音声、AUTO、読了SEの再生処理、表紙、公開処理には変更を加えていません。
