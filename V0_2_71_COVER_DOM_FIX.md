# v0.2.71
Runtime language update no longer replaces the center ending button with plain text.
It now preserves:
`<small>COVER</small><strong>表紙に戻る</strong>`
(or `Back to cover` in English), allowing the same typography as left/right.
