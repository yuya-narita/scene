# Public Player v0.3.35 — Up motion parity

- `disappear.motion: stay` remains fixed at the landing position.
- `disappear.motion: up` now moves text by 8px instead of 18px.
- Fade duration still follows `disappear.fade`.
- This change only reduces travel distance; it does not alter Scene timing.
