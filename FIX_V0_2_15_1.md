# v0.2.15.1

Cover selection/removal and work metadata edits no longer set easySourceDirty. This prevents ensureWorkingDocumentFromEasy() from rebuilding the Scene array and deleting Advanced-only image Scenes. Only actual Easy body edits trigger Scene regeneration.
