# v0.2.57 — Dynamic i18n cleanup

Final dynamic UI cleanup:
- Work Info & Cover "Optional" badge now follows JA/EN.
- AUTO REC continuation label now translates.
- AUTO REC Stop / REC Saved / Record Again now translate.
- Undo button and compact undo aria-label now translate.
- Dynamic undo notifications (Scene reordered, merged, split, deleted, resplit, sample inserted) now translate.
- Visible undo notification can update when JA/EN changes.

No changes to REC timing, undo history, Scene data, Player Core, publishing, or media handling.
